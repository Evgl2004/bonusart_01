# F23: Mailings UI v2 - Execution Report

Дата: 2026-04-24  
Статус: в работе, MVP близко к завершению

## 1) Что уже реализовано

1. Единый каркас `mailings-v2` в меню и маршрутах.
2. Hub кампаний:
2.1. KPI по кампаниям и dispatch;
2.2. фильтры списка;
2.3. операции дублирования/архива.
3. Карточка кампании:
3.1. форма параметров;
3.2. вкладки `overview/audience/runs/jobs/errors/logs`;
3.3. dry-run / run-now / retry-операции;
3.4. мастер запуска на 3 шага.
4. Workbench -> Mailings v2 bridge:
4.1. создание черновика рассылки из отбора;
4.2. сохранение snapshot фильтров;
4.3. переход сразу в `mailings-v2/campaigns/<id>`.
5. Templates:
5.1. список/фильтры;
5.2. create/edit/detail;
5.3. предпросмотр на госте.
6. Monitor:
6.1. агрегаты по статусам и провайдерам;
6.2. быстрые операции `retry failed` и `requeue waiting`;
6.3. KPI retry и отчет по последней операции.
7. Scenarios:
7.1. список с фильтрами и KPI;
7.2. ручной one-shot запуск плановых сценариев;
7.3. отчет по последнему ручному запуску.
8. Единый UX-bridge “гипотеза -> шаблон -> запуск -> мониторинг”:
8.1. общий flow-блок на ключевых экранах;
8.2. prefill кампании из шаблона (`?template_id=`).

## 2) Что проверено

1. Прогон тестов через обязательный скрипт:
1.1. `scripts/run_pytest.ps1 guests/tests/test_mailings_v2_views.py`.
2. Текущий результат:
2.1. `22 passed`.

## 3) Что осталось до полного закрытия MVP

1. Финальная UX-полировка текста и подсказок (единая терминология “кампания/запуск/доставка”).
2. Небольшой smoke-прогон в боевом UI по сценариям:
2.1. создать кампанию из Workbench;
2.2. запустить dry-run и run-now;
2.3. проверить отражение статусов в `runs/jobs/errors/monitor`.
3. Убрать/скрыть устаревшие bridge-ссылки legacy там, где новый контур уже закрывает задачу.

## 4) Риски и контроль

1. Быстрые операции monitor массовые, поэтому их применять только с осознанными фильтрами.
2. One-shot запуск сценариев требует контроля лимита (`limit_per_scenario`) при боевом объеме.
3. Для аудита спорных кейсов использовать:
3.1. `campaign_runs/jobs/errors/logs`;
3.2. monitor-агрегаты;
3.3. сценарный отчет one-shot запуска.
