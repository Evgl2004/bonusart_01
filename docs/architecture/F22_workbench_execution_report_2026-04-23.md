# F22: Workbench Execution Report (2026-04-23)

## Статус

Блок F21/F22 по экрану `guests/workbench` завершен и проверен на боевых данных.

## Что реализовано

1. Введен и стабилизирован category-window режим для `guests/workbench`:
1. при выбранной категории метрики берутся из `GuestRestaurantWindowCategoryMetrics`;
2. при пустой категории используется общий оконный слой.
2. Устранено расхождение бизнес-смысла для общего режима:
1. общий оконный слой переведен на расчет от полного чека (`OrderFact`) через дневной слой `GuestRestaurantDailyOrderFact`;
2. категорийный расчет использует `GuestOrderFocusFact + OrderFact` без постоянного полного скана сырого OLAP.
3. Исправлена регистрация managed schedule-задач в Django Q:
1. задачи из `Q_CLUSTER["schedule"]` синхронизируются в `django_q_schedule`;
2. подтверждена idempotency синхронизации.
4. Доработана логика отбора гостей в таблице workbench:
1. без сегмента таблица использует строго выбранное окно (`7/14/30/60/180`);
2. при выбранном сегменте используется профильное окно сегмента:
1. `active_30d` / `single_visit_30d` -> `30`;
2. `cooling_30_60d` -> `60` (fallback `30`);
3. `lost_60d_plus` -> `180` (fallback `60/30`).
5. Синхронизированы карточки и таблица на экране:
1. карточки `Гостей/Заказов/Визитов/Суммы/Рейтинга` считают итог по той же выборке, что и таблица;
2. учитываются сегмент, категория и дополнительные условия (логика И).
6. Оптимизирована скорость открытия страницы:
1. убран лишний повторный пересчет сегментации/матрицы в типовом сценарии;
2. добавлен `distinct` при сборке матрицы сегментов и категорий.

## Коммиты блока

1. `d0f94b7` optimize(workbench): убрать двойной пересчёт сегментов и облегчить матрицу фокусов
2. `be7252e` fix(workbench): привязать окно таблицы гостей к выбранному сегменту
3. `76513dc` fix(workbench): синхронизировать карточки с таблицей отбора гостей

## Проверки и подтверждение

1. Миграции:
1. `guests.0027_guestrestaurantwindowcategorymetrics` применена успешно.
2. Smoke/idempotency:
1. повторные прогоны `sync_window_metrics` и `sync_window_category_metrics` без дрейфа (`created=0`, `updated=0` на повторе).
3. Аудит контрольных телефонов:
1. проверка `79097376140` и `79829334252` в режиме category-window дала `PASS`;
2. метрики payload совпадают с DB.
4. Проверка спорного кейса по `+79082628849`:
1. `orders=52`, `visits=48` подтверждены как корректные;
2. выявлено 4 даты с двумя заказами в один день;
3. `sum_net` в окне совпадает с суммой дневного слоя.
5. Тесты:
1. `guests/tests/test_guest_workbench_view.py` -> passed;
2. `guests/tests/test_segments_workbench_view.py` -> passed;
3. `guests/tests/test_analytics_dashboard_view.py` -> passed.

## Текущее поведение UI (целевое)

1. Режим метрик явно показывается в шапке:
1. `Общие метрики по окну` или `Метрики по выбранной категории`.
2. Число в карточке `Гостей` соответствует числу `Найдено` в таблице в рамках одного отбора.
3. При выборе сегмента окно таблицы следует сегментной логике, при пустом сегменте следует выбранному `window_days`.

## Что дальше (следующий блок)

1. Блок F23: надежность ежедневной OLAP-цепочки (ingest -> facts -> windows):
1. автоматическая проверка свежести за последние 2 дня;
2. авто-восстановление хвоста при пропусках;
3. единый health-report для оператора.
2. Блок F24: точечная performance-диагностика workbench на боевом объеме:
1. SQL-профиль тяжелых запросов;
2. решение по индексам по факту плана/actual;
3. регламент контроля времени ответа экрана.
3. Блок F25: стабилизация UX-интерпретации метрик:
1. короткие tooltip-описания для карточек/таблицы;
2. явное описание сегментных окон прямо в UI.

