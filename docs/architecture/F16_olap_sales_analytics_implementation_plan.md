# F16. План внедрения OLAP-аналитики по чекам (поэтапно, без поломки прод-контура)

## Статус документа
Черновик для согласования и фиксации рабочей последовательности.
Опирается на архитектурный blueprint F15.

## Цель F16
Реализовать слой аналитики чеков и категорий номенклатуры на основе OLAP:
1. с минимальным риском для действующего webhook-контура;
2. с поэтапными логическими коммитами;
3. с обязательной миграционной дисциплиной Django;
4. с русскоязычной документацией и логированием.

## Базовые правила выполнения
1. Все изменения схемы только через Django migrations.
2. Каждый этап завершать зелёными проверками (`check`, `makemigrations --check`, целевые тесты).
3. Не удалять текущий рабочий контур рассылки; новый слой подключать постепенно.
4. Использовать только русские термины в документации и логах.

## Этапы внедрения (S1-S13 + S3.1/S3.2)

### S1. Техническая проверка OLAP-фильтров по идентификаторам
Задачи:
1. Подтвердить рабочую фильтрацию по:
   1. `Department.Id`;
   2. `Department.Code`;
   3. `RestaurantSection.Id` (как резерв).
2. Зафиксировать итоговое правило отбора заведений в проектной документации.
3. Подготовить эталонные примеры запросов и ответов для тестов.

Результат:
1. Документ с подтверждённым рабочим способом фильтрации.
2. Тестовые примеры для регрессии интеграции.

Коммит:
1. `docs(olap): зафиксировать рабочие фильтры и эталонные запросы`

Статус:
1. `completed` — результаты зафиксированы в `docs/architecture/F16_S1_olap_filter_validation.md`.

### S2. Миграции: журнал синхронизации чеков и сырые строки OLAP
Задачи:
1. Добавить таблицы:
   1. `olap_check_sync_journal`;
   2. `olap_sales_raw_line`.
2. Добавить индексы и ограничения идемпотентности.
3. Добавить статусную модель обработки (`new|in_progress|loaded|retry|failed|skipped`).

Результат:
1. Схема БД для безопасной дозагрузки данных из OLAP.

Коммит:
1. `feat(olap): журнал синхронизации чеков и сырые строки OLAP`

Статус:
1. `completed` — добавлены модели и миграция `0019_olapchecksyncjournal_olapsalesrawline_and_more.py`.

### S3. Миграции: базовый OLAP-справочник категорий
Задачи:
1. Добавить:
   1. `olap_category_dict`.
2. Добавить админские ограничения целостности и уникальности.

Результат:
1. Единый технический каталог категорий, обнаруженных в OLAP.

Коммит:
1. `feat(olap): базовый справочник категорий из OLAP`

Статус:
1. `completed` — добавлены модель и миграция `0020_olapdishcategorydict.py` с последующим безопасным переименованием в `olap_category_dict` на шаге `0021`.

### S3.1. Миграции: единый справочник категорий в фокусе (OLAP + виртуальные)
Задачи:
1. Добавить таблицу `olap_nomenclature_dict` (справочник номенклатуры из OLAP).
2. Добавить таблицу `virtual_category` (пользовательские виртуальные категории).
3. Добавить таблицы состава виртуальной категории:
   1. `virtual_category_nomenclature_link`;
   2. `virtual_category_olap_category_link`.
4. Добавить таблицу `focus_category` (единый бизнес-справочник для аналитики):
   1. `source_type` (`olap_direct|virtual`);
   2. взаимно-исключающие ссылки `olap_category_id` / `virtual_category_id`;
   3. `CHECK`-ограничение целостности по `source_type`.
5. Добавить таблицу ускорения расчётов `focus_category_nomenclature_resolved`.
6. Добавить индексы и ограничения уникальности для всех новых связей.

Результат:
1. Один управляемый список фокусных категорий и предрассчитанный состав по номенклатурам для быстрых ночных расчётов.

Коммит:
1. `feat(olap): реализовать S3.1 фокусные категории, виртуальные связи и resolved-таблицу`

Статус:
1. `completed` — реализовано миграцией `0021_focuscategory_focuscategorynomenclatureresolved_and_more.py`.

### S3.2. Сервисная синхронизация справочников и пересборка resolved
Задачи:
1. Реализовать сервис `sync_olap_catalogs_from_raw_lines(...)`:
   1. наполнение `olap_category_dict` и `olap_nomenclature_dict` из `olap_sales_raw_line`;
   2. fallback-ключ категории по имени (`name::<lower(name)>`), если `dish_category_id` отсутствует.
2. Реализовать сервис `rebuild_focus_category_nomenclature_resolved(...)`:
   1. разворачивание `olap_direct` и `virtual` в плоский состав номенклатур;
   2. очистка resolved-связей для неактивных фокусных категорий.
3. Добавить management-команду `sync_olap_catalogs`.
4. Добавить целевые тесты сервисов и команды.

Результат:
1. Справочники и resolved-слой поддерживаются в актуальном состоянии без ручных SQL-операций.

Коммит:
1. `feat(olap): сервисы синхронизации каталогов и пересборки resolved-слоя`

Статус:
1. `completed`.

### S4. Клиент OLAP и сервисы загрузки порциями
Задачи:
1. Реализовать сервис авторизации в iiko (`/resto/api/auth`) с кэшем ключа.
2. Реализовать сервис запросов в OLAP (`/resto/api/v2/reports/olap`).
3. Реализовать загрузку порциями с повторными попытками и журналированием ошибок.

Результат:
1. Стабильный сервис интеграции с OLAP.

Коммит:
1. `feat(olap): клиент и сервис загрузки данных порциями`

### S5. Воркер дозагрузки чеков из журнала синхронизации
Задачи:
1. Добавить management-команду воркера дозагрузки.
2. Реализовать цикл:
   1. взять новые записи журнала;
   2. сгруппировать в порции;
   3. запросить OLAP;
   4. сохранить `olap_sales_raw_line`;
   5. обновить статусы журнала.
3. Реализовать graceful shutdown и безопасное завершение.

Результат:
1. Автономный контур дозагрузки чеков без блокировки webhook.

Коммит:
1. `feat(olap): воркер дозагрузки чеков и управление статусами`

### S6. Построение факта чека (`order_fact`)
Задачи:
1. Добавить таблицу `order_fact`.
2. Реализовать преобразование из сырых строк:
   1. одна запись = один чек;
   2. корректные суммы до/после скидок;
   3. признаки купона и типа заказа.
3. Обеспечить идемпотентное обновление при повторной загрузке.

Результат:
1. Быстрый и устойчивый слой для среднего чека и частоты.

Коммит:
1. `feat(analytics): построение факта чека из сырых строк OLAP`

### S7. Дневной слой по категориям
Задачи:
1. Добавить таблицу `guest_restaurant_daily_category_fact`.
2. Реализовать ежедневный (и инкрементальный) пересчёт из `order_fact` + сырых строк.
3. Добавить контроль качества пересчёта и журнал метрик.

Результат:
1. Базовый слой для расчёта окон `7/14/30/60/180`.

Коммит:
1. `feat(analytics): дневные итоги категорий по гостям и заведениям`

### S8. Оконные метрики и рейтинг гостя
Задачи:
1. Добавить таблицу `guest_restaurant_window_metrics`.
2. Реализовать расчёт окон:
   1. `7`;
   2. `14`;
   3. `30`;
   4. `60`;
   5. `180`.
3. Реализовать вычисление `rating_score` по согласованной формуле.

Результат:
1. Готовый источник для сегментов, дашбордов и рейтингов.

Коммит:
1. `feat(analytics): оконные метрики и первичный рейтинг гостя`

### S9. Интеграция с авто-сценариями уведомлений
Задачи:
1. Добавить сценарии отбора по агрегатам:
   1. пример `meat_lover_30d`;
   2. пороги по количеству и среднему чеку.
2. Подключить к существующему контуру:
   1. `NotificationEvent`;
   2. `DispatchTask`.
3. Добавить флаги включения/выключения сценариев через БД.

Результат:
1. Бизнес-сегменты формируют рассылку на базе аналитики чеков.

Коммит:
1. `feat(scenarios): сегменты на основе чековой аналитики`

### S10. Техническая админка и операционное сопровождение
Задачи:
1. Добавить разделы админки:
   1. журнал синхронизации;
   2. приоритетные категории;
   3. метрики пересчёта.
2. Добавить фильтры и действия:
   1. повторить синхронизацию;
   2. пометить как пропущенное;
   3. экспорт диагностических записей.

Результат:
1. Поддержка и диагностика без прямых запросов к БД.

Коммит:
1. `feat(admin): техпанель синхронизации OLAP и агрегатов`

### S11. Дашборд аналитики (минимально рабочая версия)
Задачи:
1. Реализовать:
   1. топ гостей по заведению;
   2. топ категорий/блюд;
   3. динамику по окнам.
2. Добавить фильтры по заведению и периоду.

Результат:
1. Видимый бизнес-эффект и проверяемость гипотез.

Коммит:
1. `feat(dashboard): базовый дашборд по гостям, чекам и категориям`

### S12. Тестирование, нагрузочные проверки, миграционный прогон
Задачи:
1. Unit и интеграционные тесты:
   1. idempotency;
   2. повторы загрузки;
   3. ошибки OLAP;
   4. ночной пересчёт.
2. Проверка миграций на копии БД.
3. Нагрузочный прогон пересчётов.

Результат:
1. Подтверждение стабильности перед продакшеном.

Коммит:
1. `test(olap): покрытие загрузки, агрегатов и отказоустойчивости`

### S13. Ввод в эксплуатацию поэтапно
Задачи:
1. Включить контур синхронизации в режиме «только накопление».
2. Сверить цифры с выборкой из iiko.
3. Включить ночной пересчёт.
4. Включить сегменты рассылок на базе аналитики.

Результат:
1. Контролируемый запуск без регрессии действующих потоков.

Коммит:
1. `chore(release): поэтапный ввод OLAP-аналитики в эксплуатацию`

## Проверки на каждом этапе
1. `python manage.py check`
2. `python manage.py makemigrations --check`
3. целевые тесты изменённого модуля
4. smoke-команда после деплоя (без отправки сообщений пользователям)

## Definition of Done для F16
1. Синхронизация чеков из OLAP работает штатно и устойчиво к повторам.
2. Сырые данные и агрегаты согласованы и проверены на контрольной выборке.
3. Периоды `7/14/30/60/180` считаются из дневного слоя без тяжёлых онлайн-расчётов.
4. Единый справочник «категорий в фокусе» (OLAP + виртуальные) участвует в сегментации и отчётах.
5. Сценарии рассылок используют новый аналитический слой без поломки текущей очереди доставки.
