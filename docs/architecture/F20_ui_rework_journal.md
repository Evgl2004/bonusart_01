# F20. Журнал внедрения нового пользовательского интерфейса

## Статус документа
Рабочий журнал внедрения UI/UX-переработки с фиксацией этапов, решений и проверок.

## Цели
1. Вести прозрачную историю изменений по интерфейсу и аналитике.
2. Снижать риск потери контекста между итерациями.
3. Фиксировать, какие изменения уже в коде, а какие в очереди.

## Журнал этапов

### 2026-03-24. Этап 1: новая навигация + скрытие legacy
Сделано:
1. Внедрено новое рабочее меню:
   1. `Дашборд`
   2. `Сегменты`
   3. `Гости`
   4. `Категории и фокусы`
   5. `Рассылки`
   6. `Отчеты`
2. Legacy-разделы не удалены:
   1. сохранены URL;
   2. скрыты из пользовательского меню;
   3. отображаются в навигации только для `staff`.
3. Добавлены страницы-заглушки для новых разделов:
   1. `segments`
   2. `focus-categories`
   3. `reports`
4. Добавлены тесты навигации:
   1. доступность новых маршрутов;
   2. наличие новых пунктов меню;
   3. скрытие legacy-блока для обычного пользователя;
   4. отображение legacy-блока для `staff`.

Коммит:
1. `6cc8022` — `feat(ui): внедрить новую навигацию и скрыть legacy-пункты из меню`.

### 2026-03-24. Этап 1.1: стабилизация локального прогона тестов
Сделано:
1. Добавлен постоянный тестовый профиль `SQLite`:
   1. файл `loyalty_viewer/settings_test_sqlite.py`;
   2. раздельные SQLite БД для `default` и `webhooks`;
   3. отключение миграций `guests` для быстрого и стабильного локального прогона.
2. Обновлен `pytest.ini`:
   1. по умолчанию используется `DJANGO_SETTINGS_MODULE = loyalty_viewer.settings_test_sqlite`.

Комментарий по качеству:
1. SQLite-профиль закрывает проблему локальных падений на внешнем хосте `db`.
2. Для SQL-специфичных проверок финальный регресс рекомендуется выполнять в docker-контуре PostgreSQL.

### 2026-03-24. Этап 2: новый рабочий экран «Гости»
Сделано:
1. Добавлен новый экран `guests/workbench/` с фокусом на маркетинговую работу:
   1. KPI-карточки по гостям/чекам/суммам/бонусам;
   2. сегменты активности (`active_30d`, `single_visit_30d`, `cooling_30_60d`, `lost_60d_plus`);
   3. таблицы рейтинга и антирейтинга гостей;
   4. таблица сравнения заведений;
   5. визуализация гостей (`частота × средний чек`) на ECharts.
2. В навигации пункт `Гости` переключен на новый экран workbench.
3. Legacy-список гостей сохранен по прямому URL `guests/` (только как fallback).
4. Добавлен сервис сборки данных `guest_workbench.py` с понятной бизнес-логикой и русскими docstring.
5. Добавлены тесты на новый экран и обновленную навигацию.

Проверки:
1. Точечный прогон: `test_navigation_menu.py`, `test_guest_workbench_view.py`, `test_analytics_dashboard_view.py` — успешно.

### 2026-03-24. Этап 2.1: матрица «Сегменты × фокусные категории»
Сделано:
1. В сервис `guest_workbench.py` добавлен расчёт покрытия:
   1. строки — сегменты (`active_30d`, `single_visit_30d`, `cooling_30_60d`, `lost_60d_plus`);
   2. столбцы — активные фокусные категории;
   3. в каждой ячейке: количество гостей, `% внутри сегмента`, `% внутри категории`.
2. В UI (`guests/workbench.html`) добавлены:
   1. табличная матрица с наглядными карточками-ячейками;
   2. heatmap покрытия сегментов категориями.
3. Обновлены тесты workbench:
   1. зафиксирована логика заполнения матрицы;
   2. проверены ключевые кейсы по сегментам и категориям.

### 2026-03-24. Этап 2.2: интерактивный отбор гостей в workbench
Сделано:
1. В фильтры экрана `Гости: рабочий экран` добавлены:
   1. `Сегмент`;
   2. `Фокусная категория`.
2. Добавлен блок `Гости по выбранным фильтрам`:
   1. список гостей по выбранному сегменту/категории;
   2. метрики по гостю: заказы, визиты, сумма, средний чек, рейтинг;
   3. индикация ограничений по объёму выдачи (первые N строк).
3. В сервисе `guest_workbench.py` реализован отбор гостей:
   1. нормализация фильтров;
   2. выборка гостей по пересечению сегмента и фокусной категории.
4. Тесты дополнены сценариями фильтрации:
   1. проверка, что `segment_code + focus_category_code` возвращает корректного гостя.

### 2026-03-24. Этап 2.3: кликабельная матрица
Сделано:
1. В матрице `Сегменты × фокусные категории` добавлена интерактивность:
   1. клик по ячейке ставит фильтры `segment_code + focus_category_code`;
   2. после клика форма фильтров автоматически отправляется.
2. Heatmap синхронизирован с матрицей:
   1. клик по точке heatmap выполняет тот же отбор, что и клик по ячейке.
3. Улучшена визуальная обратная связь:
   1. hover-эффект;
   2. курсор и keyboard-access (Enter/Space).

### 2026-03-24. Этап 2.4: быстрое действие «Черновик рассылки»
Сделано:
1. Добавлен endpoint действий workbench:
   1. `POST /guests/workbench/actions/`.
2. Реализовано действие:
   1. `create_mailing_draft` — создание черновика рассылки по текущему отбору гостей.
3. В блоке `Гости по выбранным фильтрам` добавлена кнопка:
   1. `Создать черновик рассылки по отбору`.
4. Безопасные ограничения:
   1. при пустой выборке черновик не создаётся;
   2. при усечённой (слишком большой) выборке требуется сузить фильтр.
5. Добавлены тесты:
   1. проверка создания `Mailing` и `MailingGuest` из workbench-отбора.

### 2026-03-24. Этап 2.5: сохранение пресетов фильтра workbench
Сделано:
1. Добавлена модель пресетов:
   1. `GuestWorkbenchFilterPreset` (имя, окно, заведение, сегмент, фокусная категория, активность).
2. Добавлена миграция:
   1. `0026_guestworkbenchfilterpreset.py`.
3. В workbench добавлен блок `Сохранённые пресеты фильтров`:
   1. форма сохранения текущего набора фильтров;
   2. список кнопок-пресетов для применения в 1 клик.
4. В endpoint действий добавлено действие:
   1. `save_filter_preset` — сохранить/обновить пресет по имени.
5. Обновлены тесты:
   1. проверка сохранения пресета через action endpoint;
   2. проверка наличия пресетов в payload фильтров.

### 2026-03-24. Этап 2.6: управление пресетами в workbench
Сделано:
1. Добавлены действия для существующих пресетов:
   1. `rename_filter_preset` — переименовать пресет;
   2. `delete_filter_preset` — мягко удалить пресет (деактивация).
2. В UI пресетов добавлены:
   1. inline-форма переименования;
   2. кнопка удаления с подтверждением.
3. Добавлены тесты:
   1. переименование активного пресета;
   2. деактивация пресета при удалении.

### 2026-03-24. Этап 2.7: пресеты (активные/все) и восстановление
Сделано:
1. Расширен payload workbench:
   1. добавлен флаг `show_all_presets`;
   2. в карточку пресета добавлен признак `is_active`.
2. В сервисе пресетов реализованы режимы:
   1. по умолчанию показываются только активные пресеты;
   2. при включённом `show_all_presets` отображаются также деактивированные.
3. В action-view добавлено действие:
   1. `restore_filter_preset` — восстановление деактивированного пресета.
4. В шаблоне `workbench` добавлены:
   1. переключатель `Показывать все` в фильтрах;
   2. метка `неактивный` у архивных пресетов;
   3. кнопка `Восстановить` для неактивных пресетов.
5. В POST-формах добавлено сохранение флага `show_all_presets` при редиректах.
6. Добавлены тесты:
   1. скрытие/отображение неактивных пресетов по флагу;
   2. восстановление пресета через `restore_filter_preset`.

### 2026-03-24. Этап 3.0: рабочий экран «Сегменты»
Сделано:
1. Заглушка `segments` заменена на рабочее представление:
   1. новый view `views_segments_workbench.py`;
   2. сбор данных через `build_guest_workbench_payload` (единый источник метрик).
2. Добавлен шаблон `guests/segments_workbench.html`:
   1. фильтры (`дата среза`, `окно`, `заведение`);
   2. KPI по сегментам;
   3. таблица состава сегментов с долей от базы.
3. Для каждой строки сегмента добавлен быстрый переход:
   1. ссылка в `guests/workbench` с предустановленным `segment_code`.
4. Добавлен тест:
   1. `test_segments_workbench_view.py` — проверка состава сегментов и корректных ссылок в экран гостей.

### 2026-03-24. Этап 3.1: рабочий экран «Категории и фокусы»
Сделано:
1. Заглушка `focus-categories` заменена на рабочий экран:
   1. новый view `views_focus_categories_workbench.py`;
   2. новый шаблон `guests/focus_categories_workbench.html`.
2. В экран добавлены рабочие блоки:
   1. фильтры по дате среза, окну и заведению;
   2. таблица фокусных категорий с метриками и статусом;
   3. просмотр состава номенклатур выбранного фокуса.
3. Добавлены действия управления:
   1. `create_focus_from_olap` — создать фокус из OLAP-категории;
   2. `create_focus_from_virtual` — создать фокус из виртуальной категории;
   3. `set_focus_enabled` — включить/выключить фокус;
   4. `rebuild_focus_resolved` — пересобрать связи focus → nomenclature.
4. После изменений фокуса автоматически вызывается пересборка `focus_category_nomenclature_resolved`.
5. Добавлены тесты:
   1. `test_focus_categories_workbench_view.py` — открытие экрана, создание фокуса из OLAP, отключение фокуса с очисткой resolved-связей.

## Следующий этап
1. Переработка экрана `Гости`:
   1. рабочие вкладки (`Список` / `Сегменты` / `Визуализация`);
   2. быстрые фильтры для маркетологов;
   3. рейтинг/антирейтинг по гостям и заведениям.
